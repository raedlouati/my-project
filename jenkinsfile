pipeline {
    agent any
    stages {

        stage ('Build') {

            echo 'This is the build stage';
            hostname > 'hostname.txt';
            archiveArtifact 'hostname.txt' ;
        }
    } 



}